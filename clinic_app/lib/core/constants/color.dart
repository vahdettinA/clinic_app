import 'package:flutter/material.dart';

/// Ana renk tonu, uygulamanın birincil rengidir.
const Color primary = Color(0xff4E46E4);

/// Ana rengin açık tonu, vurgu ve arka planlarda kullanılır.
const Color primaryLight = Color(0xffA6A2F3);

/// Yüzey rengi, kart ve panel gibi öğelerin arka planı için kullanılır.
const Color surface = Color(0xffE4E6EB);

/// Yüzey renginin açık tonu, daha hafif yüzeylerde tercih edilir.
const Color surfaceLight = Color(0xffF4F5F7);

/// Genel arka plan rengi, sayfa ve uygulama arka planlarında kullanılır.
const Color background = Color(0xffFEFEFF);

/// Birincil metin rengi, okunabilirliği yüksek koyu ton.
const Color textPrimary = Color(0xff382836);
