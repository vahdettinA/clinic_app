import 'package:flutter/material.dart';

/// Küçük tüm kenarlardan eşit boşluk (padding).
const paddingSmall = EdgeInsets.all(10.0);

/// Orta tüm kenarlardan eşit boşluk (padding).
const paddingMedium = EdgeInsets.all(15.0);

/// Büyük tüm kenarlardan eşit boşluk (padding).
const paddingLarge = EdgeInsets.all(20.0);

/// Küçük yatay (sol ve sağ) boşluk.
const horizontalSmall = EdgeInsets.symmetric(horizontal: 10);

/// Orta yatay (sol ve sağ) boşluk.
const horizontalMedium = EdgeInsets.symmetric(horizontal: 15);

/// Büyük yatay (sol ve sağ) boşluk.
const horizontalLarge = EdgeInsets.symmetric(horizontal: 20);

/// Küçük dikey (üst ve alt) boşluk.
const verticalSmall = EdgeInsets.symmetric(vertical: 10);

/// Orta dikey (üst ve alt) boşluk.
const verticalMedium = EdgeInsets.symmetric(vertical: 15);

/// Büyük dikey (üst ve alt) boşluk.
const verticalLarge = EdgeInsets.symmetric(vertical: 20);
